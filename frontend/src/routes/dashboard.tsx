import { dAppName } from "../etc/globals.d";

export default function Dashboard() {
    return (
        <main style={{ padding: '1rem 0' }}>
            <h2>Trade smarter with {dAppName}</h2>
        </main>
    );
}
